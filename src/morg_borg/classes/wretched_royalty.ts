import type { ClassData } from './types'

const wretchedRoyalty: ClassData = {
  name: 'Wretched Royalty',
  stats: { str: 1, pre: 1, agi: -1 },
  gear: ['Longsword', 'Crown', 'Chainmail'],
  abilities: ['Can command commoners']
}

export default wretchedRoyalty
