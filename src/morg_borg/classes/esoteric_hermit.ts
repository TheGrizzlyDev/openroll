import type { ClassData } from './types'

const esotericHermit: ClassData = {
  name: 'Esoteric Hermit',
  stats: { pre: 2, str: -1, agi: -1 },
  gear: ['Staff', 'Scroll', 'Spellbook'],
  abilities: ['Knows one extra unclean scroll']
}

export default esotericHermit
